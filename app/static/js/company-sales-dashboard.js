(()=>{const root=document.querySelector('.csd'),svg=document.getElementById('company-sales-chart');if(!root||!svg)return;const all=JSON.parse(root.dataset.monthly||'[]'),data=all.filter(x=>Number(x.current)||Number(x.previous));if(!data.length)return;const w=820,h=270,p={l:62,r:18,t:18,b:54},max=Math.max(...data.flatMap(x=>[x.current,x.previous]),1),slot=(w-p.l-p.r)/data.length,y=v=>h-p.b-v/max*(h-p.t-p.b),fmt=v=>new Intl.NumberFormat('es-CO',{notation:'compact',maximumFractionDigits:1}).format(v),months=['ene','feb','mar','abr','may','jun','jul','ago','sep','oct','nov','dic'];svg.setAttribute('viewBox',`0 0 ${w} ${h}`);svg.innerHTML=[0,.25,.5,.75,1].map(r=>`<line class="csd-gridline" x1="${p.l}" y1="${y(max*r)}" x2="${w-p.r}" y2="${y(max*r)}"/><text class="csd-label" x="${p.l-8}" y="${y(max*r)+3}" text-anchor="end">${fmt(max*r)}</text>`).join('')+data.map((x,i)=>{const cx=p.l+i*slot+slot/2,bw=Math.min(18,slot*.28),gap=x.current-x.previous,gapLabel=`${gap>=0?'+':'−'}${fmt(Math.abs(gap))}`;return `<rect class="csd-bar-previous" x="${cx-bw-2}" y="${y(x.previous)}" width="${bw}" height="${h-p.b-y(x.previous)}" rx="2"><title>Año anterior: ${fmt(x.previous)}</title></rect><rect class="csd-bar-current" x="${cx+2}" y="${y(x.current)}" width="${bw}" height="${h-p.b-y(x.current)}" rx="2"><title>Actual: ${fmt(x.current)}</title></rect><text class="csd-label" x="${cx}" y="${h-27}" text-anchor="middle">${months[Number(x.month)-1]}</text><text x="${cx}" y="${h-11}" text-anchor="middle" font-size="9" fill="${gap<0?'#d63939':'#18864b'}">${gapLabel}</text>`}).join('')})();
