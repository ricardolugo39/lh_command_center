[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "commercial-command-center"
version = "0.2.0"
description = "Commercial Command Center"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["app*"]